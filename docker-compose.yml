version: '3.8'

services:
  dex-trading-agent:
    build: .
    ports:
      - "5173:5173"
    environment:
      - NODE_ENV=production
      - VITE_CONVEX_URL=${VITE_CONVEX_URL}
    restart: unless-stopped
    volumes:
      - ./dist:/app/dist:ro
