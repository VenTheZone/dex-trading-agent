# Convex Deployment URL (automatically generated by `npx convex dev`)
VITE_CONVEX_URL=https://your-deployment.convex.cloud

# Backend API Keys (Optional - for self-hosted deployments)
# These keys will be used as fallback if not provided through the UI
OPENROUTER_API_KEY=sk-or-v1-your-openrouter-api-key-here
CRYPTOPANIC_AUTH_TOKEN=your-cryptopanic-token-here

# Docker Deployment Flag (Optional)
# Set to "true" if running in Docker container
VITE_DOCKER_DEPLOYMENT=false
