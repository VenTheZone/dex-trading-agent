# Convex Deployment URL (automatically generated by `npx convex dev`)
VITE_CONVEX_URL=

# Optional: Backend API Key Storage (for self-hosted deployments)
# If set, these keys will be used as fallback when client-side keys are not provided
# IMPORTANT: Never commit .env.local to git - it's already in .gitignore

# OpenRouter API Key (for AI analysis)
# Get yours at: https://openrouter.ai/keys
OPENROUTER_API_KEY=

# CryptoPanic API Token (for news feed)
# Get yours at: https://cryptopanic.com/developers/api/
CRYPTOPANIC_AUTH_TOKEN=
