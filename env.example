# ============================================================================
# DeX Trading Agent - Environment Variables
# ============================================================================
# 
# IMPORTANT: Choose ONE of these options:
# 
# Option 1 (Recommended): Copy this file to .env.local
#   - Frontend (Vite) will automatically load it
#   - Backend needs .env (not .env.local)
#   - Use .env.local for frontend-only variables
#   - Use migration_python/.env for backend variables
# 
# Option 2: Copy this file to .env
#   - Both frontend and backend can use it
#   - Simpler for local development
# 
# If BOTH .env and .env.local exist:
#   - Frontend: .env.local takes precedence
#   - Backend: Only reads .env (ignores .env.local)
# 
# ⚠️ NEVER commit .env or .env.local files to git!
# ============================================================================

# Frontend Environment Variables (VITE_ prefix required)
# -------------------------------------------------------

# Python Backend API URL
# Local development: http://localhost:8000
# Docker: http://backend:8000
VITE_PYTHON_API_URL=http://localhost:8000

# Docker deployment flag (set to true when running in Docker)
VITE_DOCKER_DEPLOYMENT=false

# ============================================================================
# Backend Environment Variables
# ============================================================================
# 
# For backend configuration, use migration_python/.env instead
# See migration_python/.env.example for backend-specific variables
# 
# Backend variables include:
# - OPENROUTER_API_KEY (AI analysis)
# - CRYPTOPANIC_AUTH_TOKEN (news feed)
# - DATABASE_URL (SQLite/PostgreSQL)
# - REDIS_URL (background tasks)
# 
# Note: API keys can also be configured through the web UI
# ============================================================================
