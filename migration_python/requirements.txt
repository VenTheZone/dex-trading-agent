# Core Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-dotenv==1.0.0

# Database
sqlalchemy==2.0.23
alembic==1.12.1
psycopg2-binary==2.9.9  # PostgreSQL adapter
# OR use sqlite for local development (no additional package needed)

# Background Tasks
celery==5.3.4
redis==5.0.1

# HTTP Client
httpx==0.25.1
aiohttp==3.9.0
requests==2.31.0  # For health checks and simple HTTP requests

# WebSocket
websockets==12.0

# Crypto/Trading APIs
ccxt==4.1.0  # Multi-exchange library
eth-account==0.9.0  # For Hyperliquid wallet signing

# AI/ML
openai==1.3.0  # For OpenRouter API (OpenAI-compatible)

# Utilities
pydantic==2.5.0
pydantic-settings==2.1.0
python-jose[cryptography]==3.3.0  # JWT tokens (if needed later)
passlib[bcrypt]==1.7.4  # Password hashing (if needed later)
python-multipart==0.0.6  # Form data parsing

# Development
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.11.0
flake8==6.1.0
mypy==1.7.0